import { StyleSheet } from "react-native";

export const CommonStyles = StyleSheet.create({
    headerText: {
      fontWeight: 'bold',
      fontSize: 20,
    },
    container: {
        marginTop: 30,
    }
  });